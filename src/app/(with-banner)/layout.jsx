export default function Layout ({ children }) {
    return(
        <div>
        <marquee style={{background: '#fff', color: 'red' }}>Hecho por Seba20078</marquee>
        {children}
        </div>
    )
}